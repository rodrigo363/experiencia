#include <stdio.h>
#include <math.h>

main()
{
	int a,b,c;
printf ("introduce a number ");
scanf("%i", & b);
for(a=2;b>a;a++)
{
c=b%a;
	if (c==0){
printf("the number is not a prime");
return 0;
}
}
printf("the number is a prime");
}
